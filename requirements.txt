# Core API

fastapi>=0.130,<1.0
uvicorn[standard]>=0.30,<1.0

# Config & Validation

python-dotenv>=1.0,<2.0
pydantic[email]>=2.7,<3.0
email-validator>=2.0,<3.0

# Database

sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9.0  # PostgreSQL driver

# HTTP Clients

httpx>=0.27,<1.0
requests>=2.32,<3.0

# Testing

pytest>=8.0,<9.0

# Dashboard / UI

streamlit>=1.35,<2.0
plotly>=5.20,<6.0

# ML / Embeddings + HuggingFace Inference API

sentence-transformers>=3.0,<4.0
huggingface_hub>=0.24,<1.0


# Optional (recommended for real-world apps)

passlib[bcrypt]>=1.7,<2.0    # Password hashing
PyJWT>=2.0,<3.0              # JWT handling
gunicorn>=21.0,<22.0         # production server (Linux)
orjson>=3.10,<4.0           # faster JSON
loguru>=0.7,<1.0            # better logging
python-multipart>=0.0.9     # file uploads (FastAPI)
